#! /usr/bin/env ruby

require 'orocos'
require 'pp'

Orocos.components.each do |name|
    begin
        component = Orocos::TaskContext.get(name)
    rescue Orocos::NotFound
    end
    pp component
    puts
end

