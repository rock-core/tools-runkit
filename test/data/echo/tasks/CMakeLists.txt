# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(echoTaskLib)
ADD_LIBRARY(${ECHO_TASKLIB_NAME} SHARED 
    ${ECHO_TASKLIB_SOURCES})

add_dependencies(${ECHO_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ECHO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ECHO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ECHO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ECHO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ECHO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ECHO_TASKLIB_HEADERS}
    DESTINATION include/orocos/echo)

