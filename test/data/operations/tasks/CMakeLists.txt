# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(operationsTaskLib)
ADD_LIBRARY(${OPERATIONS_TASKLIB_NAME} SHARED 
    ${OPERATIONS_TASKLIB_SOURCES})

add_dependencies(${OPERATIONS_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${OPERATIONS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${OPERATIONS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${OPERATIONS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${OPERATIONS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${OPERATIONS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${OPERATIONS_TASKLIB_HEADERS}
    DESTINATION include/orocos/operations)

