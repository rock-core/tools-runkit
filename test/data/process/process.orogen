name 'process'
version '1.0'

toolkit do
    load "simple_types.h"
end

task_context 'Test' do
    property "prop1", "/process/Simple"
    property "prop2", "int"
end

static_deployment do
    test = task('Test').
        periodic(0.1).
        start
end
