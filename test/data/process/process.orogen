name 'process'
version '1.0'

typekit do
    load "simple_types.h"
end

task_context 'Test' do
    property "prop1", "/process/Simple"
    property "prop2", "int"
end

deployment "process" do
    test = task('process_Test', 'Test').
        periodic(0.1).
        start
end
