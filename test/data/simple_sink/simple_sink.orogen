name "simple_sink"

task_context "sink" do
    input_port "cycle", "int"
end

deployment "simple_sink" do
    task("fast_sink", "sink").
        periodic(0.001)
    task("simple_sink_sink", "sink").
        periodic(0.1)
end

